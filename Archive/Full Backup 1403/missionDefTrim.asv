mission = struct();
N = 1000;
t = linspace(1, 140, 5*N);

initTO = [zeros(1, N); zeros(1, N); linspace(0, 20, N)];
transitionTO = [linspace(0, 10, N); zeros(1, N); 20 + 30.*(1 - exp((-(t(1001:2000)/20).^3)))];

cruise = [linspace(10, 90, N); linspace(0, 20, 1000); zeros(size(t))]; 

transitionL = [linspace(90, 100, 1000); zeros(size(t)); 50 - 30.*(1 - exp((-(t/20).^3)))];
endL = [zeros(size(t)); zeros(size(t)); linspace(20, 0, 1000)];

mission.xyz = []